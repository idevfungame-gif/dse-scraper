services:
  - type: worker
    name: dse-scraper
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python live_scraper.py
    envVars:
      - key: DATABASE_URL
        sync: false
